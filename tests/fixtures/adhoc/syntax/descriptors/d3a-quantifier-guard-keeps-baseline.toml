id = "adhoc.syntax.descriptors.d3a-quantifier-guard-keeps-baseline"
lojban = "lo vei pa su'i re ve'o mlatu cu blabi"
tags = ["adhoc", "syntax", "descriptions-epoch"]

[[provenance]]
kind = "other"
name = "epoch-9 D1 ownership pin at a quantifier-bearing leading position (#830/#552)"
description = "With the camxes-exp full-sumti route DEFAULT-enabled, a quantifier-bearing leading position must still be BASELINE-owned through sumti_tail_1, silently. What this witness pins is that OWNERSHIP, held by ordered choice: sumti_base tries descriptor_with_gadri_sumti first and its sumti_tail_1 arm quantifier sumti consumes the identical extent, so the camxes-exp arm is never reached here. It does NOT isolate the !quantifier lookahead, which is measured inert on this surface and on every other one tried: see the parity record."

[expectations.syntax]
status = "success"
diagnostics = []
