id = "adhoc.syntax.bridi-tail.zantufa-tag-bo-joint-vau-less-dialect-omitted"
lojban = "mi broda pu bo brode"
tags = ["adhoc", "syntax", "bridi-tail-epoch"]

[[provenance]]
kind = "other"
name = "epoch-7 bridi-tail ownership matrix witness"
description = "D3-2 round 3: the VAU-less surface rolling Zantufa spells for its connectiveless tag-BO joint. The `!BO` reservation in `tag_term` (zantufa-1.9999.peg:31) keeps the selbri tail's own term list off the tag, so the joint is reached without the explicit VAU the round-2 witness needed. Dialect axis row 5 of 5: the default dialect behaves exactly as the explicitly empty one, so the reservation is invisible outside the gate."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [12, 14], source-text = "bo", message = "unexpected cmavo" }]
