id = "adhoc.syntax.bridi-tail.zantufa-tag-bo-joint-explicit-ku-rejected-no-features"
lojban = "mi broda pu ku bo brode"
dialect = "()"
tags = ["adhoc", "syntax", "bridi-tail-epoch"]

[[provenance]]
kind = "other"
name = "epoch-7 bridi-tail ownership matrix witness"
description = "D3-2 round 3: an explicit KU makes the tag a complete term, so the reservation never applies and the surface stays a rejection -- exactly as rolling Zantufa rejects it. Dialect axis row 3 of 3: the no-feature control."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [15, 17], source-text = "bo", message = "unexpected cmavo" }]
