id = "adhoc.syntax.bridi-tail.zantufa-tag-bo-joint-leading-term-dialect-omitted"
lojban = "mi broda do pu bo brode"
tags = ["adhoc", "syntax", "bridi-tail-epoch"]

[[provenance]]
kind = "other"
name = "epoch-7 bridi-tail ownership matrix witness"
description = "D3-2 round 3: the same joint behind a leading tail term. `do` stays a tail term of the first bridi tail and the joint owns `pu bo brode`; before the reservation the epoch-6c term-level connectorless BO arm mis-reached the tag first and the parse then failed. Dialect axis row 5 of 5: the default dialect."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [15, 17], source-text = "bo", message = "unexpected cmavo" }]
