id = "adhoc.syntax.links-jai.ce-fr3-default"
lojban = "lo broda fa je ku fa brode cu brodi"
dialect = "()"
tags = ["adhoc", "syntax", "issue-831", "links-jai-epoch"]

[[provenance]]
kind = "other"
name = "Epoch 10 C-e #831 frozen recovered row FR3"
description = "FR3: no parsed value proves the source discriminators of the wide candidate, whose JOIK continuation is broken by the skipped ku, so it answers Unproven and rejects fail-closed. Trace shows both answers at their own extents: Unproven for the wide candidate and Present for the shorter complete FA atom that actually survives, so the withdrawal lands on a real owner rather than an assumed earlier one. Default control: no C-e product exists on this axis, so the row's ownership question is never asked and the pre-existing parse stands."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [15, 17], source-text = "ku", message = "unexpected cmavo" }]

[expectations.syntax.recovered]
status = "failure"
raw = { sha256 = "05ad75311fef80940b7168ffd96f608ab398626b7e74f87db3c0a588b881ebb2" }
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [15, 17], source-text = "ku", message = "unexpected cmavo" }, { severity = "error", code = "syntax.unexpected-cmavo", byte-span = [27, 29], source-text = "cu", message = "unexpected cmavo" }]
tree = { valid-tokens = ["lo", "bróda", "fa", "bróde"], recovery-items = [{ kind = "invalid", error-index = 0, byte-spans = [[9, 11], [12, 14], [15, 17]] }, { kind = "invalid", error-index = 1, byte-spans = [[27, 29], [30, 35]] }] }
