id = "adhoc.syntax.links-jai.lr-be-mixed-unproven-added-zantufa"
lojban = "mi broda be lu mi ku li'u .e bo na ku be'o .i mi ku .i do broda"
dialect = "(zantufa)"
tags = ["adhoc", "syntax", "issue-793", "links-jai-epoch"]

[[provenance]]
kind = "other"
name = "Epoch 10 C-a complete mixed-candidate rejection"
description = "Real mixed BO connection with Added NA KU and an internally damaged LU quotation, Unproven then Added. The actual complete candidate is rejected as Unproven; final ownership and extent are independently pinned. A shorter Full, if present, is not a complete mixed-payload witness. Final field: valid plain-linked-sumti, parsed value 12..25; intended complete mixed extent 12..37. The legacy quoted-sumti fallback keeps its nested recovery item and the remaining mixed continuation is withdrawn."

[[provenance]]
kind = "other"
name = "Epoch 10 C-e disposition of the stale trailing-ku recovery"
description = "Individually dispositioned as a stale expectation, not a regression. The trailing `.i mi ku` of this input ends in a `ku` that has nothing to terminate: CLL 20.1 gives `ku` as the elidable terminator for LE and some LA descriptions, after a tense or modal to show no sumti follows, and in NA+KU, and a bare `mi` is none of those. The old expectation kept that `ku` as a valid token by synthesizing a zero-width missing required field, which is the parser inventing a description the grammar does not license; the current output skips it as invalid input instead, which is the fail-closed reading the epoch requires. Diagnostics are unchanged; only the valid-token list and that one recovery item move. This fixture was already red at the accepted head 75afada918 and was never re-measured after the C-e routing work, so the delta predates the grouped-sumti classifier."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [18, 20], source-text = "ku", message = "unexpected cmavo" }]

[expectations.syntax.recovered]
status = "failure"
raw = { sha256 = "62ae8caa2606f816c9a14c2412d232827d5e6ecaf508831db370299aa5baa341" }
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [18, 20], source-text = "ku", message = "unexpected cmavo" }, { severity = "error", code = "syntax.unexpected-cmavo", byte-span = [35, 37], source-text = "ku", message = "unexpected cmavo" }, { severity = "error", code = "syntax.unexpected-cmavo", byte-span = [49, 51], source-text = "ku", message = "unexpected cmavo" }]
tree = { valid-tokens = ["mi", "bróda", "be", "lu", "mi", "li'u", "i", "mi", "i", "do", "bróda"], recovery-items = [{ kind = "invalid", error-index = 0, byte-spans = [[18, 20]] }, { kind = "invalid", error-index = 1, byte-spans = [[27, 28], [29, 31], [32, 34], [35, 37], [38, 42]] }, { kind = "invalid", error-index = 2, byte-spans = [[49, 51]] }] }
