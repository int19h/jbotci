id = "adhoc.syntax.links-jai.lr-bei-mixed-added-unproven-field-damage-zantufa"
lojban = "mi broda be ko'a bei na ku .e bo lu mi ku li'u ku be'o .i mi ku .i do broda"
dialect = "(zantufa)"
tags = ["adhoc", "syntax", "issue-793", "links-jai-epoch"]

[[provenance]]
kind = "other"
name = "Epoch 10 C-a complete mixed-candidate rejection"
description = "Real mixed BO connection with Added NA KU and an internally damaged LU quotation, Added then Unproven. The actual complete candidate is rejected as Unproven; final ownership and extent are independently pinned. A shorter Full, if present, is not a complete mixed-payload witness. An additional KU after the complete intended payload supplies independent field damage. Final field: valid full-linked-term, parsed value 21..38; intended complete mixed extent 21..46. The surviving shorter Full includes NA KU and the elided-LIhU quotation before the damaged continuation; it does not own the explicit LIhU at the end of the complete candidate."

[[provenance]]
kind = "other"
name = "Epoch 10 C-e disposition of the stale trailing-ku recovery"
description = "Individually dispositioned as a stale expectation, not a regression. The trailing `.i mi ku` of this input ends in a `ku` that has nothing to terminate: CLL 20.1 gives `ku` as the elidable terminator for LE and some LA descriptions, after a tense or modal to show no sumti follows, and in NA+KU, and a bare `mi` is none of those. The old expectation kept that `ku` as a valid token by synthesizing a zero-width missing required field, which is the parser inventing a description the grammar does not license; the current output skips it as invalid input instead, which is the fail-closed reading the epoch requires. Diagnostics are unchanged; only the valid-token list and that one recovery item move. This fixture was already red at the accepted head 75afada918 and was never re-measured after the C-e routing work, so the delta predates the grouped-sumti classifier."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [39, 41], source-text = "ku", message = "unexpected cmavo" }]

[expectations.syntax.recovered]
status = "failure"
raw = { sha256 = "ab0aaeaad5dceb508b66757a9b8a7de56fa06316163a50a9f5601a424c890107" }
diagnostics = [{ severity = "warning", code = "syntax.warning.experimental-term-bo-connection", byte-span = [30, 32], source-text = "bo", message = "experimental syntax: BO-bound term or linked-argument connection", word-index = 8 }, { severity = "error", code = "syntax.unexpected-cmavo", byte-span = [39, 41], source-text = "ku", message = "unexpected cmavo" }, { severity = "error", code = "syntax.unexpected-cmavo", byte-span = [61, 63], source-text = "ku", message = "unexpected cmavo" }]
tree = { valid-tokens = ["mi", "bróda", "be", "ko'a", "beĭ", "na", "ku", "e", "bo", "lu", "mi", "be'o", "i", "mi", "i", "do", "bróda"], recovery-items = [{ kind = "invalid", error-index = 0, byte-spans = [[39, 41], [42, 46], [47, 49]] }, { kind = "invalid", error-index = 1, byte-spans = [[61, 63]] }] }
