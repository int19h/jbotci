id = "adhoc.syntax.links-jai.ce-sr2-zantufa"
lojban = "ko'a ke ke'e broda"
dialect = "(zantufa)"
tags = ["adhoc", "syntax", "issue-831", "links-jai-epoch"]

[[provenance]]
kind = "other"
name = "Epoch 10 C-e #831 frozen recovered row SR2"
description = "SR2 asks for a KE body with no parsed value to leave the grouped candidate Unproven and rejecting fail-closed. This fixture pins what the parser actually does, which does not satisfy that on every axis: grouped sumti is the one C-e product with no recovered evidence classifier of its own -- ownership is decided by the two strict_observe guards and the termset rejection -- so nothing refuses a grouped product whose mandatory body is a synthesized Error. Reported to the PM as an open fail-closed gap; the fixture is here so that a fix shows up as an expectation change rather than passing unnoticed. Built-in axis, where the region is skipped and no grouped sumti is claimed."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [8, 12], source-text = "ke'e", message = "unexpected cmavo" }]

[expectations.syntax.recovered]
status = "failure"
raw = { sha256 = "18bdf082b0bfde3c41e23fde09ac3f24f65183c41945cda33364f83e3427aaa9" }
diagnostics = [{ severity = "error", code = "syntax.unexpected-cmavo", byte-span = [8, 12], source-text = "ke'e", message = "unexpected cmavo" }, { severity = "error", code = "syntax.unexpected-end", byte-span = [18, 18], source-text = "", message = "unexpected end of input" }]
tree = { valid-tokens = [], recovery-items = [{ kind = "invalid", error-index = 0, byte-spans = [[0, 4], [5, 7], [8, 12], [13, 18]] }] }
