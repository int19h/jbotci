id = "adhoc.syntax.subsentence.d1-s3-gap-leading-selbri-jek-mixed-list"
lojban = "broda po'oi mi brode je poi do brodi"
dialect = "(+zantufa-terms)"
tags = ["adhoc", "syntax", "subsentence-epoch"]

[[provenance]]
kind = "other"
name = "epoch-8 subsentence ownership matrix witness"
description = "Documented gap, issue #877, the joik-joined shape of the same class as d1-s3-gap-leading-selbri-zihe-mixed-list. The connective is one camxes-exp spells, so the chain would be exp's if every clause were, but the second clause is a baseline poi the exp tanru-unit relative cannot form; the leading selbri takes clause 1 anyway and selbri_relative_clause_list never sees the list. Measured A at the epoch base 0d791fd35c, so this is a regression the epoch introduced rather than a pre-existing limitation."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.incomplete-selbri", byte-span = [36, 36], source-text = "", message = "incomplete selbri" }]
