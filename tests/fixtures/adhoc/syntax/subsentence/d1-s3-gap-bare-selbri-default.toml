id = "adhoc.syntax.subsentence.d1-s3-gap-bare-selbri-default"
lojban = "broda poi mi brode"
tags = ["adhoc", "syntax", "subsentence-epoch"]

[[provenance]]
kind = "other"
name = "epoch-8 subsentence ownership matrix witness"
description = "The retained gated omission at the default union: the selbri-level parent stays behind ZANTUFA-TERMS, so a NOI-family relative directly on a bare selbri has no route here."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.incomplete-selbri", byte-span = [18, 18], source-text = "", message = "incomplete selbri" }]
