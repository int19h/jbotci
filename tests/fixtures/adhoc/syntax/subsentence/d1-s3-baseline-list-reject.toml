id = "adhoc.syntax.subsentence.d1-s3-baseline-list-reject"
lojban = "broda poi mi brode"
tags = ["adhoc", "syntax", "subsentence-epoch"]

[[provenance]]
kind = "other"
name = "epoch-8 subsentence ownership matrix witness"
description = "R1 at S3: the relative-clause list is one the baseline owns -- a standard marker over a subbridi-shaped body -- so the selbri-level attachment returns it, and with no baseline site for it on a bare selbri the surface rejects. The faithful rolling-Zantufa reading is behind the meaning-changing flag."

[expectations.syntax]
status = "failure"
diagnostics = [{ severity = "error", code = "syntax.incomplete-selbri", byte-span = [18, 18], source-text = "", message = "incomplete selbri" }]
