# Fixtures

This directory contains cross-cutting TOML integration test cases. Keep one
test case per file, with all expectations for that case in the same file.

Use provenance-specific subdirectories so failures are easy to locate and easy
to connect back to their source material.
