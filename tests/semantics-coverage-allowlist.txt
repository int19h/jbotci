# Shrink-only semantics coverage allowlist for issue #370.
# Each sorted fixture id below currently reports "generated semantic builder does not yet support".
# Remove an id as soon as that fixture no longer has that result; never add new failures.
cll.chrestomathy.alice01
