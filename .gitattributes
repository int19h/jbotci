/vendor/lensisku/dictionary-*.json -text

# The vendored dictionary snapshot is digested byte-for-byte. Git decides
# end-of-line conversion per path, so a converted checkout would change those
# bytes on Windows and nowhere else. Check the snapshot out verbatim instead.
/crates/jbotci-dictionary-data/data/** -text
